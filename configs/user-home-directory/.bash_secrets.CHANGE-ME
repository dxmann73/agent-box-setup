# ~/.bash_secrets - API tokens and secrets (not tracked in version control)
#
# Copy this file to .bash_secrets and fill in your actual values
#
# Setup will symlink the repo version to your home directory

# Hugging Face token
export HF_TOKEN=your_huggingface_token_here
