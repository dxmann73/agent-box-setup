# ~/.bash_secrets - API tokens and secrets (not tracked in version control)
#
# Setup:
#   1. Copy this file to .bash_secrets (in same directory)
#   2. Edit .bash_secrets with your actual token values
#   3. Symlink to home: ln -sf ~/projects/dave-box-setup/configs/user-home-directory/.bash_secrets ~/.bash_secrets
#
# The .bash_secrets file is gitignored and will not be committed.

# Hugging Face token
export HF_TOKEN=your_huggingface_token_here
